// applying for everything except map
const obj = {
    name:"chahat",
    age:21
}
for (const key in obj) {
    // console.log(`${key}=>${obj[key]}`);
    
    
    
    
}

const arr = ["chahat","het","smit"]
for (const i in arr) {
    // console.log(arr[i]);
}

// const map = new Map()
// map.set('chahat',20)
// map.set('het',19)
// map.set('smit',21)

// for (const key in map) {
//     console.log(key);
// }
